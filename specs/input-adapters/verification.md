# Verification

Closure requires:

- typecheck, unit tests, catalog validation, boundary audit, and sanitize audit pass;
- Excel valid/alias/missing-field behaviors are covered;
- JSON single/array/object/invalid behaviors are covered;
- English, Spanish, acceptance criteria, incomplete stories, and mandatory QA review are covered;
- `demo:inputs` produces three validated previews with source traceability;
- existing catalogs remain valid and no Playwright execution is required.
